const header=document.getElementById("header");const toggle=document.querySelector(".menu-toggle");const menu=document.querySelector(".menu");window.addEventListener("scroll",()=>header.classList.toggle("scrolled",scrollY>20));toggle.addEventListener("click",()=>menu.classList.toggle("open"));document.querySelectorAll(".menu a").forEach(a=>a.addEventListener("click",()=>menu.classList.remove("open")));document.getElementById("year").textContent=new Date().getFullYear();

const items=[...document.querySelectorAll('.gallery-item')];const filters=[...document.querySelectorAll('.gallery-filter')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;items.forEach(item=>{const cat=item.dataset.category;item.classList.toggle('hide',f!=='all'&&cat!==f);});}));
const lb=document.querySelector('.lightbox'),lbImg=lb.querySelector('img'),lbSmall=lb.querySelector('small'),lbStrong=lb.querySelector('strong');let visible=[],current=0;function openAt(i){visible=items.filter(x=>!x.classList.contains('hide'));current=i;const b=visible[current].querySelector('.gallery-open');lbImg.src=b.querySelector('img').src;lbImg.alt=b.querySelector('img').alt;lbSmall.textContent=b.querySelector('small').textContent;lbStrong.textContent=b.querySelector('strong').textContent;lb.classList.add('open');lb.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}items.forEach(item=>item.querySelector('.gallery-open').addEventListener('click',()=>{visible=items.filter(x=>!x.classList.contains('hide'));openAt(visible.indexOf(item))}));function step(n){if(!visible.length)return;current=(current+n+visible.length)%visible.length;openAt(current)}lb.querySelector('.lightbox-prev').addEventListener('click',e=>{e.stopPropagation();step(-1)});lb.querySelector('.lightbox-next').addEventListener('click',e=>{e.stopPropagation();step(1)});function closeLb(){lb.classList.remove('open');lb.setAttribute('aria-hidden','true');document.body.style.overflow=''}lb.querySelector('.lightbox-close').addEventListener('click',closeLb);lb.addEventListener('click',e=>{if(e.target===lb)closeLb()});document.addEventListener('keydown',e=>{if(!lb.classList.contains('open'))return;if(e.key==='Escape')closeLb();if(e.key==='ArrowLeft')step(-1);if(e.key==='ArrowRight')step(1)});
